# Gamification V4 — Stage 0 Freeze Inventory

> **Status:** Freeze snapshot of the current (pre-V4) gamification surface.
> **No runtime behaviour change.** Read-only inventory, generated by
> [`scripts/gamification-inventory.cjs`](scripts/gamification-inventory.cjs:1).
> **Wallet is OUT OF SCOPE** — no wallet documents are read or written here.
>
> This document enumerates every current gamification **writer**, **reader**,
> **projection**, **fallback** and **V2/V3 path** so the V4 rewrite can prove it
> replaces all of them. It is the source of truth for Task 0.1 of
> [`docs/superpowers/plans/2026-08-05-gamification-v4-rewrite.md`](docs/superpowers/plans/2026-08-05-gamification-v4-rewrite.md).

## 1. Writers (client-side balance mutations)

Every place that mutates a gamification balance (`rewardPoints`, `lifetimeXP`,
`xpTotal`, `currentStreak`, `longestStreak`) via a Firestore write transaction.

| File | Symbol | Lines | What it writes |
|---|---|---|---|
| `src/lib/api.ts` | `claimChallenge` | 967–987 | `transaction.update(userRef, { rewardPoints, lifetimeXP })` — family challenge claim (RP + XP credit) |
| `src/lib/api.ts` | `redeemReward` | 1004–1048 | `transaction.update(userRef, { rewardPoints: currentPoints - cost, lastRedemptionId })` — reward redemption (RP debit) |
| `src/lib/api.ts` | behaviour award | 1083–1089 | `transaction.update(userRef, { rewardPoints: +points, lifetimeXP: +points })` — positive/negative behaviour credit |
| `src/lib/api.ts` | `unlockAvatar` | 3061–3090 | `transaction.update(userRef, { rewardPoints: currentPoints - cost })` — avatar unlock (RP debit) |
| `src/lib/reversalApi.ts` | reversal apply | 116 | `transaction.update(pointsUserRef, { rewardPoints: plan.points, lastReversalId })` — V2 reversal path |
| `src/lib/behaviour.ts` | `computeBehaviourBalances` | 60–88 | client-side `rewardPoints`/`lifetimeXP` delta maths (duplicated server-side by `onBehaviourEventCreated`) |

## 2. Readers (UI / projection consumers)

Every UI surface that reads gamification state to render progression, points,
streaks, levels or achievements.

| File | Reads |
|---|---|
| `src/lib/gamificationAdapters.ts` | projection resolver + legacy fallback (authoritative `xpTotal`/`level`, falls back to `users.lifetimeXP`) |
| `src/lib/achievements.ts` | `xpTotal` / `rewardPoints` for badge unlock evaluation |
| `src/components/parent/dashboard/ChildSummaryCard.tsx` | `child.rewardPoints`, `xpTotal`/level via adapter |
| `src/pages/MemberProfile.tsx` | `member.rewardPoints`, `view.xpTotal`, badge `checkUnlocked` |
| `src/pages/Dashboard.tsx` | `currentUser.rewardPoints` |
| `src/pages/Rewards.tsx` | `currentUser.rewardPoints` (redeem gating) |
| `src/pages/Family.tsx` | challenge `rewardPoints` (createChallenge / claimChallenge) |

## 3. Projections, fallbacks and V2/V3 paths

- **V3 projection:** `families/{f}/gamification_summaries/{memberId}` — authoritative
  `xpTotal`, `level`, `xpProgressInLevel`, `xpToNextLevel`, `bestStreak`,
  `currentStreak`. Consumed by `gamificationAdapters.ts`, `ChildSummaryCard.tsx`,
  `MemberProfile.tsx`, `Dashboard.tsx`, `Rewards.tsx`, `Family.tsx`, `achievements.ts`.
- **Legacy fallback (V1/V2):** `users/{id}.rewardPoints`, `.lifetimeXP`,
  `.currentStreak`, `.longestStreak` — used as a fallback source in
  `gamificationAdapters.ts` (e.g. `rawXp = projectionUsable ? summary.xpTotal : member.lifetimeXP`).
- **V2/V3 ledger:** `families/{f}/gamification_events` (XP event ledger),
  `task_occurrences` (task completion dedupe), `behaviour_events` (behaviour intent
  log), `reversals` (V2 reversal records).

## 4. Machine-generated coverage proof

The region below is produced by `scripts/gamification-inventory.cjs` and proves
the scan catches every current gamification writer, reader, projection, fallback
and V2/V3 path. Do not edit it by hand — re-run the script.

<!-- BEGIN GENERATED: gamification-v4-freeze -->

_Generated by [`scripts/gamification-inventory.cjs`](scripts/gamification-inventory.cjs:1). Do not edit by hand._

Occurrences: **2866** across **208** files.

The hand-authored sections 1–3 above enumerate every writer, reader,
projection, fallback and V2/V3 path. The operation totals below confirm
the scan classifies them (write = mutation, read = consumption).

### Totals by operation

| Operation | Count |
|---|---|
| test | 1852 |
| read | 599 |
| migrate | 204 |
| calculate | 139 |
| initialise | 58 |
| write | 14 |


<!-- END GENERATED: gamification-v4-freeze -->

## 5. Freeze guard contract (Task 0.2)

The CI guard [`scripts/gamification-freeze-guard.cjs`](scripts/gamification-freeze-guard.cjs:1)
rejects any **new** legacy gamification writer that appears outside the approved
V4 directories. It is wired into CI via `npm run ci:freeze` (and
[`.github/workflows/ci.yml`](.github/workflows/ci.yml:1)).

- **Allowed write directories (V4 system only):**
  - `src/domain/gamification/v4`
  - `functions/src/gamification/v4`
- **Forbidden writer patterns** (`FORBIDDEN_WRITER_PATTERNS`): any new
  occurrence outside the allowed directories of
  - `rewardPoints` assignment / object-property write (`rewardPoints:` or `rewardPoints =`)
  - `lifetimeXP` assignment / object-property write (`lifetimeXP:` or `lifetimeXP =`)
- **Detection scope:** the working-tree diff (added/modified lines) and any
  untracked files under `src/` and `functions/src/`. On a clean tree the guard
  exits 0; adding a `rewardPoints` write in `src/lib/api.ts` makes it exit 1.
- **Frozen legacy writers (must not gain new writers):** the entries enumerated
  in section 1 (`src/lib/api.ts`, `src/lib/reversalApi.ts`, `src/lib/behaviour.ts`).
- **Out of scope:** wallet documents, Firestore writes, and any runtime behaviour
  change. The guard is read-only.
